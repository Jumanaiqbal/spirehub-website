/**
 * Frontend Odoo status — actual API calls go through /api/* proxy (see server/).
 */
export const odooPublicConfig = {
  roomsPath: "/odoo/rooms",
  instanceUrl: "https://spire-test1.odoo.com",
};
